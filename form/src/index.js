import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(<App />);

// axiosReturn.then((res)=>{console.log("result from successful axios call",res)})
// console.log("returned from axios is",axiosReturn.promiseState)
// console.log("after calling axios.get",notes);
